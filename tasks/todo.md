# Todo — план реализации проекта

Учебный проект AgentOS: продающий лендинг + Telegram-бот с админкой.
Источник визуальных правил — `design.md`. Источник правил Claude Code — `.claude/CLAUDE.md`.

Принципы работы по каждому пункту: задача → план → действие → проверка → commit.
Отмечаем `[x]` по мере выполнения. Новые подзадачи добавляем сюда же.

---

## 1. Подготовка структуры проекта

- [x] Зафиксировать финальный стек в `.claude/CLAUDE.md` (язык, фреймворк лендинга, фреймворк бота, БД, деплой) — пока стоят плейсхолдеры в `[...]`.
- [x] Создать корневую структуру каталогов: `web/` (лендинг), `bot/` (Telegram-бот), `admin/` (если отдельный фронт админки), `db/` (миграции / схема), `docs/`.
- [x] Добавить `.editorconfig` и базовые конфиги линтеров/форматтеров под выбранный стек. (Сейчас без зависимостей: `.editorconfig`, `.gitattributes`. Линтеры/форматтеры — отложены в `docs/tooling.md` до этапов 2, 3 и 6.)
- [ ] Обновить `.gitignore` под выбранный стек (node_modules / .venv / dist / build / .next / __pycache__).
- [x] Завести `docs/architecture.md` — короткое описание модулей и их связей (1 страница).
- [ ] Создать ветку `feature/project-skeleton` и закоммитить скелет.

## 2. Frontend / лендинг

- [ ] Зафиксировать секции лендинга в `docs/landing-sections.md` (hero, манифест, методика, тарифы, CTA, FAQ, футер) — список секций, без вёрстки.
- [ ] Перенести design tokens из `design.md` в код: CSS variables / theme-файл (цвета, типографика, spacing, радиусы, тени).
- [ ] Подключить шрифты Literata, Lora, Manrope (self-host или провайдер) и описать в `docs/landing-sections.md`.
- [ ] Сверстать hero-секцию по `design.md` (центральная ось, один Primary CTA, без капса и восклицаний).
- [ ] Сверстать остальные секции по списку из `docs/landing-sections.md`, по одной за итерацию.
- [ ] Сверстать форму заявки (поля, валидация, состояния error / success согласно `design.md`).
- [ ] Прогнать адаптив по брейкпоинтам `--bp-sm / md / lg / xl` из `design.md`.
- [ ] Прогнать контраст текста и CTA против WCAG AA (визуальная проверка + инструмент).

## 3. Backend / Telegram-бот

- [ ] Зафиксировать сценарии бота в `docs/bot-flows.md`: `/start`, заявка на лендинг → уведомление в бот, FAQ, рассылки.
- [ ] Создать `bot/` со структурой: `handlers/`, `services/`, `db/`, `config/`, точка входа.
- [ ] Реализовать загрузку конфигурации из `.env` (токен, режим webhook/polling, admin ids).
- [ ] Реализовать `/start` и базовые команды (`/help`, `/menu`).
- [ ] Реализовать приём заявки от лендинга → запись в БД → уведомление админу в бот.
- [ ] Реализовать FSM для сценария оставления заявки внутри самого бота.
- [ ] Настроить логирование (структурные логи, без вывода токенов и PII).
- [ ] Локальный запуск в режиме polling с тестовым ботом.

## 4. Admin panel

- [ ] Решить тип админки: внутри Telegram-бота (команды + inline-кнопки) или отдельная веб-админка. Зафиксировать решение в `docs/admin-decision.md`.
- [ ] Описать роли и права: `admin`, `editor`, `viewer` (или минимальный набор для учебного проекта).
- [ ] Реализовать список заявок: фильтры по статусу, открытие карточки заявки (по правилам секции `Bot admin specifics` из `design.md`).
- [ ] Реализовать действия по заявке: Принять / Отложить / Отклонить (Primary / Secondary / Ghost — без `--status-danger` на кнопке отклонения).
- [ ] Реализовать модуль рассылок: черновик → предпросмотр → отправка по сегменту.
- [ ] Реализовать аудит-лог действий админа (кто, что, когда).
- [ ] Защитить вход в админку (whitelist `admin_id` или auth-флоу для веб-варианта).

## 5. Database / env

- [ ] Описать схему данных в `db/schema.md`: таблицы `users`, `leads`, `broadcasts`, `audit_log`.
- [ ] Завести миграции (инструмент по стеку: Prisma / Alembic / Drizzle / голый SQL).
- [ ] Дополнить `.env.example` всеми ключами: `BOT_TOKEN`, `DATABASE_URL`, `ADMIN_IDS`, `WEB_BASE_URL`, ключи внешних сервисов.
- [ ] Проверить `.gitignore`: `.env`, `.env.*`, `*.key`, `*.pem`, `secrets/` должны игнорироваться.
- [ ] Зафиксировать порядок локального запуска (DB up → migrate → seed → bot/web up) в `docs/local-run.md`.
- [ ] Подготовить минимальный seed-набор данных для локальной разработки.

## 6. Tests / checks

- [ ] Определить уровни проверок в `docs/testing.md`: unit (handlers, services), интеграция (handler + БД), e2e лендинга (форма заявки), smoke бота (`/start`).
- [ ] Поднять тест-раннер по стеку (Vitest / Pytest / Jest), хранить тесты рядом с модулем или в `tests/`.
- [ ] Покрыть тестами критичный путь: «отправка формы на лендинге → заявка в БД → уведомление админу».
- [ ] Покрыть тестами действия по заявке (Принять / Отложить / Отклонить) и аудит-лог.
- [ ] Добавить линтер и type-check в `package.json` / `Makefile` / `pyproject.toml` (`lint`, `typecheck`, `test`).
- [ ] Настроить pre-commit: линтер, форматтер, type-check, запрет коммита `.env`.
- [ ] Завести GitHub Actions: `lint` + `typecheck` + `test` на каждый PR (без авто-деплоя).

## 7. Deploy

- [ ] Выбрать платформу для лендинга и для бота, зафиксировать в `docs/deploy.md`.
- [ ] Описать список environment-переменных в проде и где они хранятся (никогда не в git).
- [ ] Подготовить production-конфиг бота: webhook URL, HTTPS, healthcheck.
- [ ] Подготовить production-сборку лендинга и проверить размер бандла / Lighthouse.
- [ ] Сделать staging-деплой и пройти smoke-чеклист (форма, бот `/start`, заявка попадает админу).
- [ ] Согласовать прод-деплой со мной (RED-операция по `.claude/CLAUDE.md`) и выполнить.
- [ ] После прод-деплоя: записать урок в `tasks/lessons.md` (что сработало, что переделать).

---

## Правила ведения этого файла

- Отмечаем `[x]` сразу по факту выполнения, не пачкой.
- Новые задачи — сюда же, в нужный этап.
- Корректировки/ошибки → `tasks/lessons.md` по формату «что не так / как правильно / правило на будущее».
